<?xml version="1.0" encoding="utf-8"?>
<animated-vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/ic_anim_logo">

    <!-- small -->
    <target
        android:animation="@animator/lg_anim_offset_small_sphere"
        android:name="@string/lg_path_name_sma_sp"/>
    <target
        android:animation="@animator/lg_anim_trim_end_small_sphere"
        android:name="@string/lg_path_name_sma_sp"/>

    <!-- middle -->
    <target
        android:animation="@animator/lg_anim_trim_end_middle_sphere"
        android:name="@string/lg_path_name_mid_sp"/>

    <!-- big -->
    <target
        android:animation="@animator/lg_anim_trim_end_big_sphere"
        android:name="@string/lg_path_name_big_sp"/>

    <!-- alpha -->
<!--    <target
        android:animation="@animator/lg_anim_fill_alpha"
        android:name="@string/lg_path_name_sma_sp"/>
    <target
        android:animation="@animator/lg_anim_fill_alpha"
        android:name="@string/lg_path_name_mid_sp"/>
    <target
        android:animation="@animator/lg_anim_fill_alpha"
        android:name="@string/lg_path_name_big_sp"/>-->


</animated-vector>